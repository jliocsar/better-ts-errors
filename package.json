{
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.2.5",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "extension:build": "yarn workspace better-ts-errors build"
  }
}
